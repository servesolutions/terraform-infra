# Git Commands

git init
git add .
git commit -m "message"
git remote add origin "github repo link"
git push -u origin master

git branch <branch_name> # Create new branch
git branch -a # list all branches
git branch -d <branch_name> # Delete a branch

git checkout <branch_name> # Checkout an existing branch
git checkout -b <new_branch> # Checkout and create a new branch with that name

git merge <branch_name> # Merge changes into current branch

git push <remote_URL/remote_name> <branch>
git push —all # Push all local branches to remote repository

git log # Show entire git log


# Linux Commands

sudo # performs tasks that require admin or root permissions
pwd # lets you find the path of your current directory

cd # allows you to navigate through your directories
cd ~[username]  # goes to another user’s home directory.
cd .. # moves to your previous directory .
cd - # moves to your last directory you were just in.

ls # list files and directories
ls -R # lists all the files in the subdirectories.
ls -a  # shows hidden files in addition to the visible ones.
ls -lh # shows the file sizes in easily readable formats, such as MB, GB, and TB.

mkdir # Command used to create directories in Linux

mv # Move or rename files in Linux

cat # Display file contents on the terminal

clear # Clear the terminal display

rm # Delete files or directories

rmdir # delete empty folder

df # Display disk filesystem information

du # space file/folder takes up

head # prints first ten lines of file

tail # prints last ten lines

diff # compares content of 2 files and displays difference

chmod # modifies read write and execute permission

chown # change ownership of a file

jobs # displays all the running processes

ping # checking whether a network or a server is reachable ping

touch # creates files

locate # find files -i turns off case sensitivity in database

find # search files in specific folder

grep # word filter in a file

top # View active processes live with their system usage

ps # Display active processes




# Docker Commands
